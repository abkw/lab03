dijkstra <- function(graph, init_node){
  if (!is.vector(graph)){
    stop("the graph must be a vector")
  }
  if (names(graph[1])!="v1" || names(graph[v2])!=v2 || names(graph[3])!=w){
    stop("the graph is not well structured")
  }


}
